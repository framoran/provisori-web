<%= link_to 'Nouvel article', new_article_path(), class: 'button is-primary', role: 'button' %>

<div class="grid-systems">

  <% @articles.each do |article| %>
      <div>
        <%= render partial: 'articles_to_build', locals: { article: article } %>
      </div>
    <% end %>

</div>
